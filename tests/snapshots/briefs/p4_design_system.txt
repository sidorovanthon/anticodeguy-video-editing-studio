
You are running the **design system** pass for an anticodeguy episode (`snapshot-fixture`).

Canon to read BEFORE producing output:
- `~/.agents/skills/hyperframes/SKILL.md` §"Step 1: Design system" — the canonical
  workflow, the design.md-vs-composition-rules separation, and the HARD-GATE that
  fires if you reach for `#333` / `#3b82f6` / `Roboto`.
- `~/.agents/skills/hyperframes/visual-styles.md` — the 8 named presets (Swiss
  Pulse, Velvet Standard, Deconstructed, Maximalist Type, Data Drift, Soft
  Signal, Folk Frequency, Shadow Cut). If one fits, name it as `style_name` and
  base the YAML on its tokens.
- `~/.agents/skills/hyperframes/references/design-picker.md` — the spec-format
  for a design.md (YAML frontmatter + Overview / Colors / Typography / Layout /
  Elevation / Components / Do's and Don'ts prose sections).
- `~/.agents/skills/hyperframes/house-style.md` §"Lazy Defaults to Question" —
  the AI design tells you must NOT default to.

Read those sections via the `Read` tool. The shape, naming, and avoidance rules
are load-bearing and we do not paraphrase them here.

Inputs:
- Strategy from Phase 3 (shape / takes / grade / pacing / length): `{"shape": "hook-problem-payoff", "takes": ["take-1"], "grade": "neutral, mild contrast lift", "pacing": "fast-SLOW-fast", "length_estimate_s": 30.0}`
- EDL beats already chosen for the cut (use these as `beat_visual_mapping` keys): `["HOOK", "PROBLEM", "PAYOFF"]`

Outputs:

1. **Return the DESIGN.md body in the `design_md` field** of the structured
   `DesignDoc` response below. Do NOT call the `Write` tool. Do NOT touch
   the filesystem. The orchestrator writes the file to `/tmp/snapshot-fixture/episode/hyperframes/DESIGN.md`
   from your returned body (HOM-232 — state-first artifacts, Step B of the
   HOM-230 epic). The spec format (YAML frontmatter keys + prose section
   names) is defined in `references/design-picker.md` — follow it exactly
   as written there. Do not rely on remembered structure; read the canon
   file. The downstream beat sub-agents will consume this content as their
   source of truth — make every section operational, not aspirational.

2. **Return ONLY JSON** matching the `DesignDoc` schema shown below. Substance
   bounds are non-negotiable — the deterministic gate `gate:design_ok` rejects
   anything below them and re-dispatches you with the violation list:
   - `refs` ≥ 2 (named designers, brands, or movements with the specific aspect
     being referenced)
   - `alternatives` ≥ 1 (a real direction you considered and rejected, with a
     concrete reason — not a strawman)
   - `anti_patterns` ≥ 3 (specific don'ts for THIS brand, not generic
     "avoid neon")
   - `beat_visual_mapping` ≥ 1 entry per beat in the EDL — the beat label and a
     one-sentence treatment (palette emphasis, type scale, motion intensity,
     decoratives).

**Palette pre-bake (orchestrator-house extension).** The canonical palette
shape (see `~/.agents/skills/hyperframes/references/design-picker.md` —
"Colors" section) is `palette: [{role, hex}]`, an open-ended flat list. We
do not change that shape. We DO require the palette to **pre-bake gradient-
stop derivatives** so downstream beat sub-agents (`p4_beat`) never need to
improvise off-palette hexes for gradients, shadow tints, or hover states.
The HF canon `gate:design_adherence` (see `~/.agents/skills/hyperframes/SKILL.md`
§"Design Adherence" item 1 — strict palette ⊆) rejects any hex in
`index.html` that isn't literally in `palette[*].hex`. Pre-baking derivatives
upstream eliminates that whole class of redispatch.

For each named *base* color (background, foreground, accent, midtone — name
them as the design calls for), emit:

* the canonical hex with the bare role (e.g. `background`)
* 1-2 darker tints (gradient bottoms / shadow stops) ~10% / ~20% darker,
  named with a structured `-stop-N` suffix (e.g. `background-stop-1`,
  `background-stop-2`)
* 1-2 lighter tints (highlight / rim stops) ~10% / ~20% lighter, named with
  a `-highlight-N` suffix (e.g. `background-highlight-1`)

The palette grows from ~3-5 entries to ~8-12 entries. Shape stays flat
`{role, hex}` — the gate reads `palette[*].hex` and is shape-agnostic; it
just needs the derived hexes present.

```json
{
  "style_name": "Swiss Pulse",
  "palette": [
    {"role": "background",             "hex": "#0a0a0a"},
    {"role": "background-stop-1",      "hex": "#080808"},
    {"role": "background-stop-2",      "hex": "#050505"},
    {"role": "background-highlight-1", "hex": "#141414"},
    {"role": "foreground",             "hex": "#f0f0f0"},
    {"role": "foreground-stop-1",      "hex": "#d8d8d8"},
    {"role": "foreground-highlight-1", "hex": "#ffffff"},
    {"role": "accent",                 "hex": "#0066FF"},
    {"role": "accent-stop-1",          "hex": "#0052cc"},
    {"role": "accent-stop-2",          "hex": "#003d99"},
    {"role": "accent-highlight-1",     "hex": "#3385ff"}
  ],
  "typography": [
    {"role": "headline", "family": "Helvetica Neue", "weight": 700, "size": "5rem"},
    {"role": "body",     "family": "Inter",          "weight": 400, "size": "1rem"}
  ],
  "refs": [
    {"label": "Josef Müller-Brockmann", "description": "Grid-locked compositions; numeric stats dominate the frame."},
    {"label": "Stripe Press",            "description": "Editorial restraint — large headlines, generous gutters, no decoration for decoration's sake."}
  ],
  "alternatives": [
    {"name": "Folk Frequency", "rejected_because": "Tone is festive/community; this episode reads as analytical and would feel trivialized."}
  ],
  "anti_patterns": [
    "No gradient text — looks like every AI-generated landing page.",
    "No cyan-on-dark or purple-to-blue gradients — neon AI default.",
    "No equal-weight centered layouts — must lead the eye to the stat."
  ],
  "beat_visual_mapping": [
    {"beat": "HOOK",     "treatment": "Tight typographic close-up, accent at full saturation, ambient grid lines breathing."},
    {"beat": "PROBLEM",  "treatment": "Stat at 7rem dominates frame; foreground tinted toward accent at 8% opacity."},
    {"beat": "PAYOFF",   "treatment": "Wide reveal with hairline rules; entrance staggered 120ms; no exit tween, transition handles."}
  ],
  "design_md_path": "/tmp/snapshot-fixture/episode/hyperframes/DESIGN.md",
  "design_md": "---\nstyle: Swiss Pulse\npalette:\n  background: \"#0a0a0a\"\n  foreground: \"#f0f0f0\"\n  accent: \"#0066FF\"\n---\n\n# Overview\n\nGrid-locked editorial design...\n\n# Colors\n\n... (full body, YAML frontmatter + prose sections per references/design-picker.md)"
}
```

The `design_md` field carries the FULL body (YAML frontmatter + every prose
section the design-picker spec mandates) as a single JSON-escaped string —
backslashes, double-quotes, and newlines need standard JSON escaping. The
snippet above is illustrative; your output is ~3–8 KB of real content. The
orchestrator's extractor accepts the first valid fenced ```json``` code
block (see `graph/src/edit_episode_graph/backends/_schema_extract.py`); take
care not to truncate.

Do not include prose outside the JSON. Hex colors must be CSS-valid (`#RGB`,
`#RRGGBB`, or `#RRGGBBAA`). The HARD-GATE in the canon you just read is real —
if you write `#333` or `Roboto` you have skipped it.
