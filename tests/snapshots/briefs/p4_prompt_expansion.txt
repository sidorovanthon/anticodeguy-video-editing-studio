

You are running the **prompt expansion** pass for an anticodeguy episode (`snapshot-fixture`).

Canon to read BEFORE producing output (use the `Read` tool — do NOT rely on
remembered structure; the section names, ordering, and per-scene format are
load-bearing and we do not paraphrase them here):

- `~/.agents/skills/hyperframes/SKILL.md` §"Step 2: Prompt expansion" — the
  canonical workflow and the contract that this step is *never pass-through*.
- `~/.agents/skills/hyperframes/references/prompt-expansion.md` — the six
  required output sections, the per-scene format, the "atmosphere /
  secondary motion / micro-details / object-level transitions" rules.
- `~/.agents/skills/hyperframes/references/beat-direction.md` — per-beat
  planning format (concept / mood / depth layers / choreography verbs /
  transition out) you must use for each scene.
- `~/.agents/skills/hyperframes/references/video-composition.md` — density,
  scale, and color-presence rules for the video medium (8–10 elements per
  scene, etc.).
- `~/.agents/skills/hyperframes/house-style.md` — Background Layer,
  typography, color, motion rules that apply to every scene.

Inputs:
- DESIGN.md body — INLINED below; cite its EXACT hex values, font names, and
  tokens; do NOT invent palette or typography. Do NOT call `Read` on
  `/tmp/snapshot-fixture/episode/hyperframes/DESIGN.md` — the file is not on disk while this node runs
  (HOM-265: state-first artifacts, Step E partial; the materializer writes
  it at chain end). Treat the body verbatim as if you had just read it:

  ```design.md
  # DESIGN.md — snapshot fixture

(palette + typography body)

  ```

- User prompt seed (the original brief; enrichment must preserve everything in
  it — atmosphere, ambient motion, micro-details are added on top, never as
  replacement): `"Editorial restraint; analytical not festive."`
- Strategy from Phase 3 (shape / takes / grade / pacing / length): `{"shape": "hook-problem-payoff", "takes": ["take-1"], "grade": "neutral, mild contrast lift", "pacing": "fast-SLOW-fast", "length_estimate_s": 30.0}`
- EDL beats already chosen for the cut — these are the SCENES the expansion
  must produce per-scene specs for: `["HOOK", "PROBLEM", "PAYOFF"]`
- Transcript JSON body — INLINED below (HOM-279: state-first; do NOT call
  `Read` on `/tmp/snapshot-fixture/episode/edit/transcripts/raw.json`). Per-word timing source for pacing
  references and beat duration anchoring:

  ```transcript.json
  {"edl_hash":"snapshot-fixture","words":[{"text":"hello","start":0.0,"end":0.5,"type":"word"},{"text":"world","start":0.6,"end":1.1,"type":"word"}]}
  ```

Profile & brand context (operator-authored overlay ON TOP of canon — brand
wins on conflict; honor canonical opt-outs, never replace canon):

## PROFILE PLACEHOLDER — ## Rhythm template
(snapshot fixture)




Outputs:

1. **Return the expansion body in the `expanded_prompt` field** of the
   structured `ExpandedPrompt` response below. Do NOT call the `Write`
   tool. Do NOT touch the filesystem. The orchestrator writes the file
   to `/tmp/snapshot-fixture/episode/hyperframes/.hyperframes/expanded-prompt.md` from your returned body (HOM-233 —
   state-first artifacts, Step B of the HOM-230 epic). Section names,
   ordering, per-scene format, and length guidance are defined in
   `references/prompt-expansion.md` and `references/beat-direction.md` —
   follow them exactly as written there. Do not rely on remembered
   structure; read the canon files.

2. **Return ONLY JSON** matching this shape (no prose outside the JSON):

```json
{
  "expanded_prompt_path": "/tmp/snapshot-fixture/episode/hyperframes/.hyperframes/expanded-prompt.md",
  "expanded_prompt": "# Scene 1 — HOOK\n\n... (full body, six required sections + per-scene specs per references/prompt-expansion.md)"
}
```

The `expanded_prompt` field carries the FULL body (every required section
+ per-scene spec) as a single JSON-escaped string — backslashes,
double-quotes, and newlines need standard JSON escaping. The snippet above
is illustrative; your output is typically ~5–15 KB of real content. The
orchestrator's extractor accepts the first valid fenced ```json``` code
block (see `graph/src/edit_episode_graph/backends/_schema_extract.py`);
take care not to truncate.

Canonical opt-out honored: this node produces the expansion only. Do NOT
generate composition HTML, scene sub-agents, or DESIGN.md here — those are
later graph nodes (`p4_beats`, etc.). Stop after returning the JSON.
