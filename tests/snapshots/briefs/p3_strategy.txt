

You are running the **strategy** pass for an anticodeguy episode (`snapshot-fixture`).

Canon — VERBATIM, pulled live from the skill at run time (follow exactly; do
NOT `Read` these sections again — they are inlined below in full):

=== CANON — VERBATIM from the live skill: video-use SKILL.md §The process — Step 4 (Propose strategy) (follow exactly; do not summarize) ===
## CANON PLACEHOLDER — ## The process → item 'Propose strategy'
(snapshot fixture; the verbatim section is resolved at run time)
=== end CANON: video-use SKILL.md §The process — Step 4 (Propose strategy) ===

=== CANON — VERBATIM from the live skill: video-use SKILL.md §Cut craft (techniques) (follow exactly; do not summarize) ===
## CANON PLACEHOLDER — ## Cut craft (techniques)
(snapshot fixture; the verbatim section is resolved at run time)
=== end CANON: video-use SKILL.md §Cut craft (techniques) ===

=== CANON — VERBATIM from the live skill: video-use SKILL.md §Color grade (when requested) (follow exactly; do not summarize) ===
## CANON PLACEHOLDER — ## Color grade (when requested)
(snapshot fixture; the verbatim section is resolved at run time)
=== end CANON: video-use SKILL.md §Color grade (when requested) ===


Orchestrator-house clarification on `grade`: it is image/CDL direction only,
NOT audio or codec settings — loudnorm/codec/fps live entirely in canon
`render.py` and are not operator-tunable through strategy.

Inputs:
- Packed transcript path: `/tmp/snapshot-fixture/episode/edit/takes_packed.md`
- Pre-scan slips/avoid list JSON: `[{"reason": "filler", "range_s": [1.2, 1.45]}]`
- Packed transcript content:

```text
[take-1] 00:00:00.500 --> 00:00:04.200
Snapshot fixture transcript line one.
[take-1] 00:00:04.500 --> 00:00:08.100
Snapshot fixture transcript line two.

```



Return ONLY JSON matching:

```json
{"shape":"...","takes":["..."],"grade":"...","pacing":"...","length_estimate_s":30.0}
```

Phase 3 is cuts + grade only. Animation and subtitles belong to Phase 4; the
schema has no fields for them. Do not include prose outside the JSON.
