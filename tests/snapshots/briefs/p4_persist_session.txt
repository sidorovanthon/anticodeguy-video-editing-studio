
You are running the **Phase 4 persist-session** pass for an anticodeguy
episode (`snapshot-fixture`). This appends a Phase 4 Session block to the same
`project.md` file Phase 3 writes to — session numbering is monotonic across
the file regardless of which phase produced the block.

Canon (format — mandatory read): `~/.claude/skills/video-use/SKILL.md`
§"Memory — `project.md`". The Session block heading shape, bullet field
names, and field semantics are load-bearing; we do not paraphrase canon
in this brief.

Phase 4 context: this run produced a HyperFrames composition (DESIGN.md,
expanded prompt, plan with beats, captions block, assembled root
`index.html`). Read `~/.agents/skills/hyperframes/SKILL.md` if you need
context on what those artifacts are; the field shape you write into
`project.md` still comes from the video-use §Memory canon above.

Target file (for context — DO NOT write it yourself; the orchestrator
appends from your returned body): `/tmp/snapshot-fixture/episode/edit/project.md`

Existing `project.md` body (HOM-265: inlined below — do NOT call `Read`
on the path above; the materializer is the single writer and the body
already on disk, if any, is supplied here. On a fresh first run the
body is empty — that's your signal that `N = 1`):

```project.md

```

Inputs from this run (already-decided artifacts; map them into the canon
fields per your reading of §Memory — do not re-derive):

- DESIGN.md path: `/tmp/snapshot-fixture/episode/hyperframes/DESIGN.md`
- Expanded prompt path: `/tmp/snapshot-fixture/episode/hyperframes/.hyperframes/expanded-prompt.md`
- Plan JSON: {"shape": "hook-problem-payoff", "beats": [{"id": "hook", "title": "Hook", "duration_s": 6.9}, {"id": "payoff", "title": "Payoff", "duration_s": 5.0}]}
- Beats summary JSON (one entry per assembled scene): [{"beat_id": "hook", "title": "Hook", "duration_s": 6.9, "scene_path": "/tmp/snapshot-fixture/episode/hyperframes/compositions/scene-hook.html", "status": "assembled"}, {"beat_id": "payoff", "title": "Payoff", "duration_s": 5.0, "scene_path": "/tmp/snapshot-fixture/episode/hyperframes/compositions/scene-payoff.html", "status": "assembled"}]
- Captions block path (may be empty if captions skipped): `/tmp/snapshot-fixture/episode/hyperframes/captions.html`
- Assembled index.html path: `/tmp/snapshot-fixture/episode/hyperframes/index.html`
- Gate results JSON (only Phase 4 records — gate:design_ok, gate:plan_ok,
  gate:static_guard, etc.): [{"gate": "gate:design_ok", "ok": true, "iteration": 1}, {"gate": "gate:plan_ok", "ok": true, "iteration": 1}, {"gate": "gate:static_guard", "ok": true, "iteration": 1}]
- Today's date (UTC, YYYY-MM-DD): 2026-05-10

Procedure:

1. Use the inlined `project.md` body above (HOM-265: do NOT call `Read`
   on `/tmp/snapshot-fixture/episode/edit/project.md` — the materializer is the single writer
   and we already supplied its current content). Empty body = no prior
   sessions.
2. Determine the next session number `N` by scanning existing
   `## Session <N> — <date>` headings (per canon shape) in the inlined
   body and incrementing past the highest. If no prior Session blocks
   exist, `N = 1`. This is monotonic across the file's lifetime —
   re-runs do not overwrite or merge prior blocks, and Phase 3 + Phase 4
   share the same numbering space.
3. Compose one new Session block dated `2026-05-10` per canon §Memory.
   Source the field contents from the Phase 4 inputs above. Mark this
   block clearly as a Phase 4 (composition) session — e.g., in the
   block's primary descriptor field per canon, mention Phase 4 / HF
   composition so a future reader can distinguish it from Phase 3 edit
   sessions. Where canon's field names imply edit-phase semantics (EDL
   ranges, render iterations), substitute the Phase 4 analogues you can
   read from the inputs (beats, gates) — preserve canon's *field shape*
   while adapting the *content domain*.
4. Return JSON only:

```json
{
  "session_block": "## Session <N> — <date>\n\n<canonical bullet fields per §Memory>",
  "persisted_at": "<ISO 8601 timestamp at the moment you finish composing>",
  "session_n": <N>
}
```

The `session_block` field carries the markdown body of the NEW block ONLY
(starting with the `## Session N — <date>` heading) — NOT the merged file
contents. JSON-escape newlines, backslashes, and double-quotes as standard.

Notes:

- The runtime appends `session_block` (preceded by a blank line) to
  `/tmp/snapshot-fixture/episode/edit/project.md`. Do NOT call the `Write` tool — it is not in
  your `allowed_tools` (HOM-237 — state-first artifacts, Step B6 of the
  HOM-230 epic). Preserve every byte of pre-existing content above your
  appended block is the orchestrator's responsibility, not yours.
- The runtime overwrites `persisted_at` with its own UTC timestamp
  post-HOM-224 (identity-only state — paths are derived from slug, not
  echoed); your value is a placeholder to satisfy schema validation.

Do not include prose outside the JSON block.
