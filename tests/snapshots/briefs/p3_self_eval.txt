

You are running the **self-eval** pass for an anticodeguy episode (`snapshot-fixture`).

Canon — VERBATIM, pulled live from the skill at run time (follow exactly; the
check list and threshold guidance are load-bearing — do NOT `Read` these
sections again, they are inlined below in full):

=== CANON — VERBATIM from the live skill: video-use SKILL.md §The process — Step 7 (Self-eval before showing the user) (follow exactly; do not summarize) ===
## CANON PLACEHOLDER — ## The process → item 'Self-eval'
(snapshot fixture; the verbatim section is resolved at run time)
=== end CANON: video-use SKILL.md §The process — Step 7 (Self-eval before showing the user) ===

=== CANON — VERBATIM from the live skill: video-use SKILL.md §Hard Rules (production correctness — non-negotiable) — esp. HR 3 audio-pop / 30ms fade (follow exactly; do not summarize) ===
## CANON PLACEHOLDER — ## Hard Rules (production correctness — non-negotiable)
(snapshot fixture; the verbatim section is resolved at run time)
=== end CANON: video-use SKILL.md §Hard Rules (production correctness — non-negotiable) — esp. HR 3 audio-pop / 30ms fade ===


Inputs (resolve via the `Read` / `Bash` tool — paths, not contents, are
provided here):
- Rendered output: `/tmp/snapshot-fixture/episode/edit/final.mp4`
- EDL JSON: `/tmp/snapshot-fixture/episode/edit/edl.json`
- Cut boundaries (output-time seconds where adjacent ranges meet): [6.9, 11.9]
- Source-time cut boundaries per range: [{"index": 0, "source": "take-1", "start": 0.5, "end": 7.4}]
- timeline_view.py helper: `/home/op/.claude/skills/video-use/helpers/timeline_view.py`

Follow the inspection procedure in the VERBATIM Step 7 above — what to
sample, where to run `timeline_view`, and what visual/audio cues to
look for are all defined there. Use the cut boundaries above as the
±1.5s anchor points; invoke `timeline_view.py` via Bash at the path
above.

Two orchestrator-house overrides on top of canon Step 7 (because Phase
4 owns overlays + captions, not Phase 3):

- Skip Hard Rule 1 (subtitle-behind-overlay) checks — no subtitles in
  this cut.
- Skip Hard Rule 4 (overlay misalignment) checks — no overlays in
  this cut.
- Do **not** run `ffprobe` for duration verification; the downstream
  `gate:eval_ok` does that deterministically and re-flags any drift.

Output: ONLY a JSON document matching this schema. Do not include prose
outside the JSON block.

```json
{
  "issues": [
    {"kind": "visual_discontinuity|audio_pop|grade_drift|other",
     "location": "cut[<i>]@<output_time_s>",
     "severity": "blocker|note",
     "note": "<one-line description>"}
  ],
  "passed": <true if no blocker-severity issues, else false>
}
```

If everything looks clean, return `{"issues": [], "passed": true}`.
