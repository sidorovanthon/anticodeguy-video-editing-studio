# Secrets
.env

# Tool installs
tools/video-use/
node_modules/
.venv/
__pycache__/
*.pyc

# Heavy media — kept out of repo
incoming/*
!incoming/.gitkeep

library/music/*
!library/music/.gitkeep

episodes/*/source/*
!episodes/*/source/.gitkeep
episodes/*/stage-1-cut/master.mp4
episodes/*/stage-1-cut/raw.mp4
episodes/*/stage-1-cut/transcript.json
episodes/*/stage-1-cut/edit/
episodes/*/stage-2-composite/assets/master.mp4
episodes/*/stage-2-composite/assets/music.*
episodes/*/stage-2-composite/preview.mp4
episodes/*/stage-2-composite/final.mp4
# HyperFrames per-episode skill outputs (animation-map, contrast-report);
# regenerated by run-stage2-compose.sh, not authored content.
episodes/*/stage-2-composite/.hyperframes/
# HyperFrames inspect cache — regenerated on every render, not authored.
episodes/*/stage-2-composite/.inspect.json

# Claude Code / Claude Agent SDK local plugin & cache state
.claude/

# FROZEN pilot — pre-6a archive, never edited; ignored to keep git status clean.
# See AGENTS.md and standards/pipeline-contracts.md for FROZEN status.
# Strictly scoped to the one historical pilot path so future episodes that
# accidentally regenerate hf-project/ still surface as untracked.
episodes/2026-04-27-desktop-software-licensing-it-turns-out/stage-2-composite/hf-project/

# HyperFrames render scratch dirs at repo root
work-*/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
