"""Factory for class-1 deterministic nodes (subprocess wrappers).

Populated in v1 — see spec §6.1. v0 carries this file as an anchor for the import
surface so later versions don't have to restructure the package.
"""
