{# Self-eval task descriptor. References canon — does NOT embed canon prose.
   The dispatched sub-agent reads SKILL.md at call time. #}
You are running the **self-eval** pass for an anticodeguy episode (`{{ slug }}`).

Canon: `~/.claude/skills/video-use/SKILL.md` §"The process" — Step 7
("Self-eval (before showing the user)") and Hard Rule 3 (audio-pop /
30ms fade). Read those sections before evaluating — the check list and
threshold guidance are load-bearing and we do not paraphrase them here.

Inputs (resolve via the `Read` / `Bash` tool — paths, not contents, are
provided here):
- Episode dir: `{{ episode_dir }}`
- Rendered output: `{{ final_mp4_path }}`
- EDL JSON: `{{ edl_path }}`
- Cut boundaries (output-time seconds where adjacent ranges meet): {{ cut_boundaries_json }}
- Source-time cut boundaries per range: {{ source_cut_boundaries_json }}
- timeline_view.py helper: `{{ timeline_view_path }}`

Procedure:

1. For each cut boundary in the **rendered output**, invoke
   `timeline_view.py {{ final_mp4_path }} <t-1.5> <t+1.5>` to render a
   ±1.5s window filmstrip+waveform PNG. Inspect the PNG (read it as an
   image) for visual discontinuity / flash / jump and for waveform
   spikes that suggest the 30ms audio fade did not absorb the cut.
2. Sample first 2s, last 2s, and 2–3 mid-points to check grade
   consistency across the rendered output.

Phase 3 orchestrator policy: this episode has **no overlays** and **no
subtitles** in the cut (those land in Phase 4). Skip canon's Hard Rule 1
(subtitle-behind-overlay) and Hard Rule 4 (overlay misalignment) checks
— they do not apply here.

ffprobe duration vs EDL `total_duration_s` is enforced deterministically
by the downstream `gate:eval_ok`; you do **not** need to run ffprobe.

Output: ONLY a JSON document matching this schema. Do not include prose
outside the JSON block.

```json
{
  "issues": [
    {"kind": "visual_discontinuity|audio_pop|grade_drift|other",
     "location": "cut[<i>]@<output_time_s>",
     "severity": "blocker|note",
     "note": "<one-line description>"}
  ],
  "passed": <true if no blocker-severity issues, else false>
}
```

If everything looks clean, return `{"issues": [], "passed": true}`.
