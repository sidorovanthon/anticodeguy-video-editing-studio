{# Strategy task descriptor. HOM-377: canon pulled VERBATIM from the live
   skill at run time (`canon.*`). #}
{% from "_macros.j2" import canon_section %}
You are running the **strategy** pass for an anticodeguy episode (`{{ slug }}`).

Canon — VERBATIM, pulled live from the skill at run time (follow exactly; do
NOT `Read` these sections again — they are inlined below in full):

{{ canon_section("video-use SKILL.md §The process — Step 4 (Propose strategy)", canon.process_propose_strategy) }}
{{ canon_section("video-use SKILL.md §Cut craft (techniques)", canon.cut_craft) }}
{{ canon_section("video-use SKILL.md §Color grade (when requested)", canon.color_grade) }}

Orchestrator-house clarification on `grade`: it is image/CDL direction only,
NOT audio or codec settings — loudnorm/codec/fps live entirely in canon
`render.py` and are not operator-tunable through strategy.

Inputs:
- Packed transcript path: `{{ takes_packed_path }}`
- Pre-scan slips/avoid list JSON: `{{ pre_scan_slips_json }}`
- Packed transcript content:

```text
{{ takes_packed_text }}
```

{% if strategy_revisions and strategy_revisions|length > 0 %}
**Operator revision feedback** — incorporate every item below into the new
strategy. The previous strategy you proposed was reviewed by the operator
who returned the following corrections (oldest → newest). The latest item
takes precedence on any conflict:

```json
{{ strategy_revisions_json }}
```
{% endif %}

Return ONLY JSON matching:

```json
{"shape":"...","takes":["..."],"grade":"...","pacing":"...","length_estimate_s":30.0}
```

Phase 3 is cuts + grade only. Animation and subtitles belong to Phase 4; the
schema has no fields for them. Do not include prose outside the JSON.
