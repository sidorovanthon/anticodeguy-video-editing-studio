{# Per-episode captions authoring task. HOM-377: non-negotiable caption canon
   sections are pulled VERBATIM from the live skill at run time (`canon.*`);
   the rest of captions.md + other refs stay as `Read` citations. #}
{% from "_macros.j2" import canon_section %}
You are authoring **the captions block** for an anticodeguy episode (`{{ slug }}`).

The block is a single HTML fragment (with embedded `<style>` + `<script>`) that
`p4_assemble_index` will inline into the root `index.html` between the beat
fragments and the v4 visibility shim. Pattern A composition — the captions
container is a regular `<div>` (no `data-composition-id`, no `<template>`
wrapper) that sits inside the root composition's `<body>`.

Inputs (HOM-265 + HOM-279: BOTH the DESIGN.md and the transcript JSON
bodies are INLINED below — neither file is read from disk while this
node runs. DESIGN.md is post-HOM-265 state-first (materializer writes
it at chain end); the transcript body was hoisted into state by
`glue_remap_transcript` at the Phase 3 → Phase 4 boundary (HOM-279).
Do NOT call `Read` on either path):

- DESIGN.md — visual identity (palette, typography, refs, anti-patterns).
  Captions MUST inherit the design system: pick fonts + colors from
  this body, not from canon defaults:

  ```design.md
  {{ design_md_body }}
  ```

- Transcript JSON (`{{ transcript_json_filename }}`) — word-level
  transcript with `text`, `start`, `end` per token. The body is
  inlined below; consume it directly. Do NOT call `Read` on
  `{{ transcript_json_path }}`. Group tokens into caption groups per
  canon §"Word Grouping". Reference the filename
  (`{{ transcript_json_filename }}`) verbatim in the HTML so
  `gate:captions_track` can detect the captions track:

  ```transcript.json
  {{ transcript_json_body }}
  ```

Composition viewport: `{{ data_width }} × {{ data_height }}` — choose the
portrait vs. landscape positioning rules from canon §"Positioning".

Total composition duration: `{{ data_duration_s }}` seconds (used for the
GSAP root timeline length).

Outputs:

1. **Return the captions block body in the `html` field** of the structured
   `CaptionsOutput` response below. Do NOT call the `Write` tool. Do NOT
   touch the filesystem. The orchestrator writes the file to
   `{{ captions_block_path }}` from your returned body (HOM-235 —
   state-first artifacts, Step B of the HOM-230 epic). The shape, hard
   rules, and process steps below define what the body MUST contain.

2. **Return ONLY JSON** matching the `CaptionsOutput` schema (no prose
   outside the JSON):

```json
{ "html": "<div id=\"captions-layer\" class=\"captions-layer\">\n  <style>…</style>\n  …\n  <script>(function(){ … })();</script>\n</div>" }
```

The `html` field carries the FULL fragment (scoped `<style>`, all per-group
`<div>`s, IIFE-wrapped `<script>` with the GROUPS literal + GSAP timeline +
canon-mandated `tl.set(...)` kills) as a single JSON-escaped string —
backslashes, double-quotes, and newlines need standard JSON escaping. The
snippet above is illustrative; your output is typically ~5–15 KB of real
content. The orchestrator's extractor accepts the first valid fenced
```json``` code block (see
`graph/src/edit_episode_graph/backends/_schema_extract.py`); take care not
to truncate.

Canon — VERBATIM, pulled live from the skill at run time. These are the
non-negotiable caption rules; the loader re-pulls them on every canon change,
so this IS the current canon. Follow exactly — do NOT `Read` these sections
again (they are below in full):

{{ canon_section("hyperframes references/captions.md §Positioning", canon.positioning) }}
{{ canon_section("hyperframes references/captions.md §Text Overflow Prevention", canon.text_overflow_prevention) }}
{{ canon_section("hyperframes references/captions.md §Caption Exit Guarantee", canon.caption_exit_guarantee) }}
{{ canon_section("hyperframes references/captions.md §Constraints", canon.constraints) }}

Additional canon to `Read` ON DEMAND (only the non-negotiable sections above are inlined; fetch these yourself when relevant):
1. `~/.agents/skills/hyperframes/references/captions.md` — the REST of the file: §"Style Detection" (4 dimensions × 5 tone profiles — detect tone from the transcript and pick a profile from §"Script-to-Style Mapping"); §"Per-Word Styling" (brand/ALL CAPS/numbers/emotional/CTA per-word emphasis); §"Word Grouping" (2–6 words per group; break on sentence boundaries, 150ms+ pauses, max word count).
2. `~/.agents/skills/hyperframes/references/transcript-guide.md` — transcript shape reference (the `text`/`start`/`end` array your code consumes).
3. `~/.agents/skills/hyperframes/references/dynamic-techniques.md` — only if the chosen tone profile calls for karaoke / clip-path / slam-words / scatter exits. Read on demand, not by default.
4. `~/.agents/skills/hyperframes/SKILL.md` §"Animation Guardrails" — applies to the captions timeline too (≥ 3 distinct eases, deterministic seek).

Output shape (return as the `html` string in the `CaptionsOutput` JSON above):

```html
<div id="captions-layer" class="captions-layer">
  <style>
    /* ALL rules prefixed with #captions-layer (no bare class selectors) */
    #captions-layer { position: absolute; inset: 0; pointer-events: none; overflow: visible; }
    #captions-layer .cg { /* per-group styles inherited from DESIGN.md typography + palette */ }
    /* per-word emphasis classes — brand, allcaps, number, cta, emotional */
  </style>
  <!-- one <div class="cg" id="cg-0">…words…</div> per group, all initially opacity:0 -->
  <script>
    (function() {
      // Reference transcript: {{ transcript_json_filename }}
      // The fragment ships its grouping table inline (the transcript JSON is
      // resolved at build time, not fetched at runtime — Phase 4 Pattern A
      // is a single self-contained HTML).
      var GROUPS = [
        // { id: "cg-0", text: "...", start: 0.0, end: 1.2,
        //   words: [{ text, start, end, emphasis: "brand"|"allcaps"|... }] },
        // ...one per caption group
      ];
      window.__captionTimelines = window.__captionTimelines || {};
      var tl = gsap.timeline({ paused: true });
      GROUPS.forEach(function (group) {
        var el = document.getElementById(group.id);
        if (!el) return;
        // Entrance via tl.fromTo (motion-principles.md §"Load-Bearing GSAP Rules" — never tl.from)
        tl.fromTo(el, /* from */ {}, /* to */ {}, group.start);
        // Exit + hard kill (canon §"Caption Exit Guarantee" — non-negotiable).
        // The exit window MUST end at or before the NEXT group's entrance —
        // see GROUPS-table invariant in the hard-rules section below
        // (group[i].end ≤ group[i+1].start). Two captions on screen at the
        // same instant is the HOM-210 / HOM-215 symptom: "fragments of two
        // captions stacked at t=11".
        tl.to(el, { opacity: 0, scale: 0.95, duration: 0.12, ease: "power2.in" }, group.end - 0.12);
        tl.set(el, { opacity: 0, visibility: "hidden" }, group.end);
      });
      // Self-lint per canon §"Caption Exit Guarantee"
      GROUPS.forEach(function (group, gi) {
        var el = document.getElementById(group.id);
        if (!el) return;
        tl.seek(group.end + 0.01);
        var computed = window.getComputedStyle(el);
        if (computed.opacity !== "0" && computed.visibility !== "hidden") {
          console.warn("[caption-lint] group " + gi + " still visible at t=" + (group.end + 0.01).toFixed(2) + "s");
        }
      });
      tl.seek(0);
      window.__captionTimelines["captions"] = tl;
      // Nest under the root timeline so it advances with the composition's seek.
      var root = window.__timelines && window.__timelines["root"];
      if (root) root.add(tl, 0);
    })();
  </script>
</div>
```

Hard rules (orchestrator-house, derived from canon — surfaced so you don't miss them):
- **Use the inlined DESIGN.md body first.** Captions inherit typography (font-family, weight) and palette
  (foreground + accent) from the design system. Do not pick fonts/colors out of canon. Do NOT `Read` DESIGN.md — it is not on disk while this node runs (HOM-265).
- **Reference `{{ transcript_json_filename }}` in the HTML** (a comment is enough). This
  is the precondition for `gate:captions_track` to detect the captions track.
- **`tl.set(...)` deterministic kill at `group.end` for every group.** Canon §"Caption
  Exit Guarantee" — non-negotiable.
- **`position: absolute` on the captions container.** Never `position: relative`. Never
  `left: 50%; transform: translateX(-50%)` on the container — clips at composition edges.
- **One caption group visible at a time.** Word grouping per canon: 2–3 words for hype,
  3–5 for conversational, 4–6 for measured/calm — choose the band from the detected tone.
- **Caption-block exit MUST land at or before the next block's entrance —
  the GROUPS table is a non-overlapping ordered partition.** For every
  consecutive pair `(group[i], group[i+1])` in the static GROUPS literal,
  `group[i].end ≤ group[i+1].start` MUST hold. Two captions on screen at
  the same instant is the HOM-210 / HOM-215 symptom (fragments of two
  captions stacked at t=11s on the canonical fixture); root cause is a
  GROUPS table whose entries overlap in time, NOT a missing kill-tween
  (the canon-mandated `tl.set(... { opacity: 0, visibility: "hidden" },
  group.end)` is necessary but not sufficient — if `group[i+1].start <
  group[i].end`, the next entrance fires while the previous block is
  still mid-exit and they paint together for that overlap window).
  When transcript timestamps from a single word stream produce naturally
  abutting groups, set `group[i].end = min(group[i].end, group[i+1].start)`
  — clamp the exit to the next entrance rather than letting them overlap.
  This is the captions-track instance of the same Pattern A pair contract
  that HOM-213 enforces on per-scene content. Canon: `~/.agents/skills/hyperframes/references/captions.md`
  §"Constraints" (bullet "One group visible at a time") + §"Caption Exit
  Guarantee"; `~/.agents/skills/hyperframes/references/motion-principles.md`
  bullet "Hard-kill every scene boundary, not just captions."
- **CSS scoping**: every rule prefixed with `#captions-layer`. No bare class selectors.
- **`tl.fromTo()` for entrances** (motion-principles.md §"Load-Bearing GSAP Rules"). Never `tl.from()`,
  never bare `gsap.from()`. Non-linear seek breaks otherwise.
- **Determinism**: no `Math.random()`, no `Date.now()`. Group/word arrays are static
  literals in the script.
- **No `<script src="..."`>** — root scaffold supplies GSAP once.
- **IIFE-wrap the entire script body.** The captions block is inlined into the root `index.html` between the scaffold's `<script>` (which has top-level `const tl = ...`) and the v4 visibility shim. ALL `<script>` blocks in the same HTML share one script-lexical environment, so an un-IIFE'd `var tl` here collides with the scaffold's `const tl` and `hyperframes validate` halts with `Identifier 'tl' has already been declared`. Every `var`/`let`/`const` you declare MUST live inside `(function() { … })();` exactly as in the output shape above.
- **Avoid the literal substring `repeat:` followed by ` -1`** (HF lint regex
  false-positive). Use `repeat: 999` or omit.

Process:
1. Use the inlined DESIGN.md body above for typography + palette
   (HOM-265: do NOT call `Read` on `{{ design_md_path }}`).
2. Use the inlined transcript JSON body above for the word stream and
   tone detection (HOM-279: do NOT call `Read` on
   `{{ transcript_json_path }}`).
3. Follow the VERBATIM canon above (inlined in full — do NOT `Read` those sections again); `Read` the on-demand references only when relevant.
4. Group words per §"Word Grouping" using the band that matches the detected tone.
5. Apply per-word emphasis per §"Per-Word Styling" — brand/product names, ALL CAPS,
   numbers/statistics, emotional keywords, calls-to-action.
6. Return the fragment shape above in the `html` field of the `CaptionsOutput`
   JSON (single fenced ```json``` code block, no prose outside). Do NOT call
   `Write` — it is not in your `allowed_tools`. The orchestrator writes the
   file from your returned body.

Do NOT modify the root `index.html`, do NOT touch sibling `compositions/*.html`, do NOT
add `<script src=...>` tags. Stay inside the captions block.
