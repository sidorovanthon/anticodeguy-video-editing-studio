"""Jinja2 task templates for class-3 (LLM) nodes.

Populated in v2 — see spec §6.3 / §6.5. Each LLM node owns a `<node_name>.j2`
file in this directory; templates render the short task descriptor passed to
the chosen `LLMBackend`.
"""
